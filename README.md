To run DWC adviser do following:

```
from dwc_adviser import MainAdviser

adviser = MainAdviser()

```

Then for each query you can call

```
adviser.make_suggestion(diary_entry)
```

which will return movie title.
